spring:
  application:
    name: leave-management

  datasource:
    url: jdbc:postgresql://localhost:5432/leave_management
    username: postgres
    password: 123

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true
